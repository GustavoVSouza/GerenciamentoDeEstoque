{
  "compilerOptions": {
    "target": "ES6",                // Compila para a versão ES6 do JavaScript
    "module": "commonjs",           // Utiliza o sistema de módulos CommonJS (padrão para Node.js)
    "outDir": "./dist",             // Diretório onde os arquivos JavaScript compilados serão colocados
    "rootDir": "./src",             // Diretório raiz onde os arquivos TypeScript estão localizados
    "strict": true,                 // Habilita todas as opções de verificação de tipo estrito
    "esModuleInterop": true,        // Permite importações de módulos que não são nativos do ES6
    "skipLibCheck": true,           // Ignora a verificação de tipo em arquivos de declaração de biblioteca
    "forceConsistentCasingInFileNames": true // Garante consistência na capitalização dos nomes de arquivos
  },
  "include": ["./src/**/*.ts", "src/db.js", "src/produtos.js"],        // Inclui todos os arquivos TypeScript no diretório src
  "exclude": ["node_modules"]       // Exclui o diretório node_modules da compilação
}
